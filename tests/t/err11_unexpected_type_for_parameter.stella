// ERROR_UNEXPECTED_TYPE_FOR_PARAMETER: lambda param type mismatch with expected
language core;

fn apply(f : fn(Nat) -> Nat) -> Nat {
  return f(0)
}

fn main(n : Nat) -> Nat {
  return apply(fn(x : Bool) { return 0 })
}
